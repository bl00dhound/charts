apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  CNFG_DB__CONNECTION__HOST: {{ include "postgresql.fullname" . }}
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
data:
  POSTGRES_DB: {{ printf .Values.postgresql.postgresqlDatabase | b64enc | quote }}
  POSTGRES_USER: {{ printf .Values.postgresql.postgresqlUsername | b64enc | quote }}
  POSTGRES_PASSWORD: {{ printf .Values.postgresql.postgresqlPassword | b64enc | quote }}
